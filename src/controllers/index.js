const loginController = require('./login.controller');
const userController = require('./user.post.controller');
const userGetController = require('./user.get.controller');

module.exports = {
  loginController,
  userController,
  userGetController,
};