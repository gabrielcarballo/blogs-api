const loginController = require('./login.controller');
const userController = require('./user.controller');
const categoryService = require('./categories.controller');

module.exports = {
  loginController,
  userController,
  categoryService,
};